%h1 Registered institutions

%table
  %tr
    %th Name
    %th Abbreviation
    %th Website
    %th Country
    %th Team manager

  - @institutions.each do |institution|
    %tr
      %td= institution.name
      %td= institution.abbreviation
      %td= institution.website
      %td= institution.country
      %td
        - if institution.user.nil?
          =link_to 'Add team manager', "/users/sign_up?insititution_id=#{institution.id}"

%br

%p Cannot find your institution ? #{link_to 'Click here to add your institution now', new_institution_path}
