%h1 Observers
= form_for @registration, url: users_observers_path, html: { class: 'form-horizontal edit_observers' } do |form|
  -if @registration.errors.any?
    #error_explanation
      %h4= "#{pluralize(@registration.errors.count, "error")} prohibited these observer profiles from being saved:"
      %ul
        - @registration.errors.full_messages.each do |msg| 
          %li= msg
  - i = 0
  = form.fields_for :observers do |f|
    - i+=1
    %legend Observer #{i}
    = render partial: 'participant_form', locals: {f: f} 
    %hr
  = form.submit 'Update'